// Sort Exercise
// Instructions: Create an array of at least five numbers in random order. Write the code to sort this array in ascending order.

const numbers = [42, 7, 13, 29, 3];

numbers.sort((a, b) => a - b, 0);

console.log(numbers);
